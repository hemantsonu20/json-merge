<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <artifactId>json-merge</artifactId>
    <version>2.0.1-SNAPSHOT</version>
    <name>json-merge</name>
    <description>A lightweight library to merge two json objects into a single json object.</description>
    <url>https://github.com/hemantsonu20/json-merge</url>

    <parent>
        <groupId>com.github.hemantsonu20</groupId>
        <artifactId>hemantsonu20-parent</artifactId>
        <version>2.0.3</version>
    </parent>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <jackson.version>3.0.3</jackson.version>
        <junit.jupiter.version>5.9.2</junit.jupiter.version>
    </properties>

    <scm>
        <connection>scm:git:https://github.com/hemantsonu20/json-merge.git</connection>
        <developerConnection>scm:git:https://github.com/hemantsonu20/json-merge.git</developerConnection>
        <tag>${project.version}</tag>
        <url>https://github.com/hemantsonu20/json-merge</url>
    </scm>

    <dependencies>

        <dependency>
            <groupId>tools.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.jupiter.version}</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <configuration>
                    <links>
                        <link>https://www.javadoc.io/doc/tools.jackson.core/jackson-databind/${jackson.version}</link>
                    </links>
                    <reportOutputDirectory>docs/apidocs</reportOutputDirectory>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
